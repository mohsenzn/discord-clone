import React from 'react';

const SideBarChannel = ({id,channel}) => {
    return (
        <div className="sidebarChannel"> 
        <h4>
            <span className="sidebarChannel__hash">#YouTube</span>
            
        </h4>
    </div>
    );
};

export default SideBarChannel;